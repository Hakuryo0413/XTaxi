import React from 'react';
import MapComponent from './MapComponent';


const Book: React.FC = () => {
  return (
    <div style={{ height: '100vh', width: '100%' }}>
      <MapComponent />
    </div>
  );
};

export default Book;
